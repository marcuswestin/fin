List
	- make templates have views
	- make template lists

alpha
	(?) templates should say the type of item references, also possibly which template to use to render it
	animate planels
	keystroke handling
	email + password => labels
	
	

storage
	User => labels
	label => view over items
	every now and then, dump items to database

beta
	filtered pubsub?
	
Demo
 	a corner of the screen is a mini-version of the UI, driven by computer. Simulates doing actions, which are reflected in users UI. Uses FE pubsub of items only - no BE


Done
	X mutations!
	X create db
	X split panels
	X button for creating new item
	X Animate panels when clicking them
